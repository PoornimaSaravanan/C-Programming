#include <stdio.h>
int main()
{
  int n = 10, i, sum = 0;
    for(i = 1; i <= n; i++)
    {
      sum = sum + i;
    }

    printf("Sum of series = %d", sum);
    return 0;
}
------------------------------------------------
Output: Sum of series-55
